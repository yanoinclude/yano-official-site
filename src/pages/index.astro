---
import { homeContent } from "@/content/pages/home";
import { placeholderImages, siteConfig } from "@/content/pages/site";
import BaseLayout from "@/layouts/BaseLayout.astro";
import kominkaRoofRestoration from "@/assets/images/home/kominka-roof-restoration.webp";
import lemonJuiceDisplay from "@/assets/images/home/lemon-juice-display.webp";

const services = [
  {
    number: "01",
    title: "古民家再生・活用",
    body: "古い家の原型を尊重しながら手を入れ、人が集まり、次の使い方が生まれる場所へ。",
    href: "/kominka",
    image: placeholderImages.house,
    alt: "中島の古民家を手入れする様子",
  },
  {
    number: "02",
    title: "農園再生・管理",
    body: "使われなくなりそうな畑を引き継ぎ、木と実が次の季節へ続くよう手入れします。",
    href: "/lemon-farm",
    image: placeholderImages.farm,
    alt: "レモンの木を手入れする様子",
  },
  {
    number: "03",
    title: "商品づくり・販売",
    body: "畑で受け取った恵みに、地域との関係が始まる届け方をつくります。",
    href: "/product",
    image: placeholderImages.bottle,
    alt: "週末移住レモン果汁",
  },
  {
    number: "04",
    title: "体験・共同企画",
    body: "島の内と外が出会い、一緒に手を動かせる時間や企画を育てます。",
    href: "/visit",
    image: placeholderImages.repair,
    alt: "島の人や仲間と活動する様子",
  },
];

const works = [
  {
    label: "PLACE",
    title: "築100年の古民家",
    image: placeholderImages.house,
    alt: "手入れを重ねてきた築100年の古民家",
    href: "/kominka",
  },
  {
    label: "FARM",
    title: "100本のレモン畑",
    image: placeholderImages.farm,
    alt: "中島のレモン畑で木を手入れする様子",
    href: "/lemon-farm",
  },
  {
    label: "PRODUCT",
    title: "週末移住レモン果汁",
    image: placeholderImages.bottle,
    alt: "週末移住レモン果汁の瓶",
    href: "/product",
  },
];
---

<BaseLayout
  title="地域の宝を、現役に。"
  description="古い家や使われなくなりそうな畑を手入れし、もう一度、人と仕事が巡る場所へ。愛媛県松山市・中島で地域の宝を現役に戻す、ヤノの公式サイトです。"
>
  <section class="top-hero" aria-labelledby="hero-title">
    <img src={homeContent.hero.image} alt={homeContent.hero.imageAlt} />
    <div class="top-hero__shade"></div>
    <div class="top-hero__copy">
      <p>島の続きを、手入れする。</p>
      <h1 id="hero-title">地域の宝を、<br />現役に。</h1>
      <span>愛媛県松山市・中島</span>
    </div>
  </section>

  <section class="purpose section-shell reveal-section" id="about">
    <header class="section-heading">
      <p>WHAT WE DO</p>
      <h2>地域に残るものを、<br />次の営みへ。</h2>
    </header>
    <div class="purpose__body">
      <p class="purpose__lead">
        古い家や使われなくなりそうな畑を手入れし、もう一度、人と仕事が巡る場所へ。ヤノは、地域の宝を現役に戻す仕事をしています。
      </p>
      <div class="purpose__themes">
        <article>
          <figure>
            <img
              src={kominkaRoofRestoration.src}
              alt="中島の古民家の屋根で工具を手に改修作業をする人"
              loading="lazy"
            />
          </figure>
          <div>
            <span>01 / PLACE</span><h3>古い家に、<br />人の気配を。</h3><p>
              古民家再生・活用
            </p>
          </div>
        </article>
        <article>
          <figure>
            <img
              src={placeholderImages.farm}
              alt="中島のレモン畑"
              loading="lazy"
            />
          </figure>
          <div>
            <span>02 / FARM</span><h3>眠りかけた畑に、<br />実りを。</h3><p>
              レモン農園
            </p>
          </div>
        </article>
        <article>
          <figure>
            <img
              src={lemonJuiceDisplay.src}
              alt="レモンと一緒に並ぶ週末移住レモン果汁の瓶"
              loading="lazy"
            />
          </figure>
          <div>
            <span>03 / PRODUCT</span><h3>地域の恵みに、<br />届け方を。</h3><p>
              商品・体験づくり
            </p>
          </div>
        </article>
      </div>
    </div>
  </section>

  <section class="about-block reveal-section">
    <figure>
      <img
        src={placeholderImages.about}
        alt="中島のレモン畑で作業する矢野さとし"
        loading="lazy"
      />
    </figure>
    <div class="about-block__copy">
      <p class="section-label">ABOUT YANO</p>
      <h2>島の続きを、<br />手入れする。</h2>
      <p>
        築100年の古民家と、100本のレモンの木。受け継いだものの原型を尊重し、必要な分だけ手を入れる。そこから生まれる場所や実りを、人、仕事、商品、そして次の担い手へつないでいきます。
      </p>
      <strong
        >築100年の古民家を2円で譲り受け、<br
        />100本のレモンの木を引き継ぎました。</strong
      >
      <a class="text-arrow" href="/about"
        >ヤノについて知る <span aria-hidden="true">→</span></a
      >
    </div>
  </section>

  <section class="services section-shell reveal-section" id="services">
    <header class="section-heading">
      <p>SERVICES</p>
      <h2>できること</h2>
    </header>
    <div class="services__list">
      {
        services.map((service) => (
          <a href={service.href}>
            <span>{service.number}</span>
            <figure>
              <img src={service.image} alt={service.alt} loading="lazy" />
            </figure>
            <h3>{service.title}</h3>
            <p>{service.body}</p>
            <i aria-hidden="true">→</i>
          </a>
        ))
      }
    </div>
  </section>

  <section class="works section-shell reveal-section" id="works">
    <header class="section-heading">
      <p>PROJECTS</p>
      <h2>手入れしてきたもの</h2>
    </header>
    <div class="works__grid">
      {
        works.map((work) => (
          <a href={work.href}>
            <figure>
              <img src={work.image} alt={work.alt} loading="lazy" />
            </figure>
            <p>{work.label}</p>
            <h3>{work.title}</h3>
          </a>
        ))
      }
    </div>
  </section>

  <section class="journal section-shell reveal-section" id="journal">
    <header class="section-heading">
      <p>JOURNAL</p>
      <h2>記録</h2>
    </header>
    <div class="journal__intro">
      <p>
        家、畑、人との出会い。手入れするなかで起きたことを、写真と言葉で残しています。
      </p>
      <a class="text-arrow" href="/columns"
        >記録を読む <span aria-hidden="true">→</span></a
      >
    </div>
    <div class="journal__grid">
      {
        homeContent.columns.items.slice(0, 3).map((item) => (
          <a href={`/columns/${item.slug}/`}>
            <figure>
              <img src={item.image} alt={item.imageAlt} loading="lazy" />
            </figure>
            <p>{item.category}</p>
            <h3>{item.title}</h3>
          </a>
        ))
      }
    </div>
  </section>

  <section class="closing reveal-section" id="contact">
    <img
      src={homeContent.closing.image}
      alt={homeContent.closing.imageAlt}
      loading="lazy"
    />
    <div class="closing__shade"></div>
    <div class="closing__copy">
      <p>この島に、帰ってこられる風景を。</p>
      <h2>地域の宝を、<br />もう一度動かしませんか。</h2>
      <a href={`mailto:${siteConfig.contact.email}`}
        >相談する <span aria-hidden="true">→</span></a
      >
    </div>
  </section>

  <script>
    const targets = document.querySelectorAll(".reveal-section");
    if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) {
      targets.forEach((target) => target.setAttribute("data-in-view", "true"));
    } else {
      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.setAttribute("data-in-view", "true");
              observer.unobserve(entry.target);
            }
          });
        },
        { threshold: 0.08 },
      );
      targets.forEach((target) => observer.observe(target));
    }
  </script>
</BaseLayout>

<style>
  :global(html) {
    scroll-behavior: smooth;
  }
  .section-shell {
    width: min(calc(100% - 8rem), 1180px);
    margin-inline: auto;
  }
  .top-hero {
    position: relative;
    min-height: min(860px, 100svh);
    color: #fff;
    overflow: hidden;
  }
  .top-hero > img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .top-hero__shade,
  .closing__shade {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      90deg,
      rgba(17, 30, 20, 0.56),
      rgba(17, 30, 20, 0.05) 72%
    );
  }
  .top-hero__copy {
    position: absolute;
    z-index: 1;
    left: max(4rem, calc((100% - 1180px) / 2));
    bottom: clamp(4rem, 10vh, 7rem);
  }
  .top-hero__copy p {
    margin: 0 0 1.2rem;
    font-size: 0.82rem;
    letter-spacing: 0.18em;
  }
  .top-hero__copy h1 {
    margin: 0;
    font-family: var(--font-island-heading);
    font-size: clamp(3.2rem, 7vw, 6.8rem);
    font-weight: 400;
    line-height: 1.18;
    letter-spacing: 0.08em;
  }
  .top-hero__copy span {
    display: block;
    margin-top: 2rem;
    font-size: 0.75rem;
    letter-spacing: 0.15em;
  }
  .section-heading > p,
  .section-label {
    margin: 0 0 1.2rem;
    color: var(--island-muted);
    font-size: 0.68rem;
    letter-spacing: 0.22em;
  }
  .section-heading h2,
  .about-block h2 {
    margin: 0;
    font-family: var(--font-island-heading);
    font-size: clamp(2rem, 4vw, 3.5rem);
    font-weight: 400;
    line-height: 1.5;
    letter-spacing: 0.06em;
  }
  .purpose {
    display: grid;
    grid-template-columns: 0.7fr 1.3fr;
    gap: clamp(4rem, 10vw, 10rem);
    padding-block: clamp(7rem, 12vw, 11rem);
  }
  .purpose__lead {
    max-width: 42rem;
    margin: 0 0 5rem;
    font-family: var(--font-island-heading);
    font-size: clamp(1.15rem, 2vw, 1.55rem);
    line-height: 2;
    letter-spacing: 0.05em;
  }
  .purpose__themes {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    border-top: 1px solid var(--island-border);
  }
  .purpose__themes article {
    padding: 1.5rem 1rem 0 0;
  }
  .purpose__themes span {
    color: var(--island-muted);
    font-size: 0.65rem;
  }
  .purpose__themes h3 {
    margin: 1.8rem 0 1.2rem;
    font-family: var(--font-island-heading);
    font-size: clamp(1.2rem, 2vw, 1.65rem);
    font-weight: 400;
    line-height: 1.65;
  }
  .purpose__themes p {
    margin: 0;
    color: var(--island-muted);
    font-size: 0.78rem;
  }
  .about-block {
    display: grid;
    grid-template-columns: 1fr 1fr;
    min-height: 720px;
    background: #e9e8df;
  }
  .about-block figure {
    margin: 0;
    min-height: 600px;
  }
  .about-block figure img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .about-block__copy {
    align-self: center;
    max-width: 35rem;
    padding: clamp(4rem, 8vw, 8rem);
  }
  .about-block__copy > p:not(.section-label) {
    margin: 2rem 0;
    line-height: 2.1;
  }
  .about-block__copy strong {
    display: block;
    padding-block: 1.5rem;
    border-block: 1px solid rgba(40, 52, 42, 0.25);
    font-family: var(--font-island-heading);
    font-size: 1.05rem;
    font-weight: 400;
    line-height: 1.9;
  }
  .text-arrow {
    display: inline-flex;
    gap: 2rem;
    align-items: center;
    margin-top: 2rem;
    padding-bottom: 0.5rem;
    border-bottom: 1px solid currentColor;
    color: inherit;
    font-size: 0.82rem;
    text-decoration: none;
  }
  .services,
  .works,
  .journal {
    padding-block: clamp(7rem, 12vw, 11rem);
  }
  .services__list {
    margin-top: 4rem;
    border-top: 1px solid var(--island-text);
  }
  .services__list a {
    display: grid;
    grid-template-columns: 4rem minmax(14rem, 0.8fr) 1.2fr 2rem;
    gap: 2rem;
    align-items: center;
    padding: 2.5rem 0;
    border-bottom: 1px solid var(--island-border);
    color: inherit;
    text-decoration: none;
  }
  .services__list span,
  .services__list p {
    color: var(--island-muted);
    font-size: 0.78rem;
  }
  .services__list h3 {
    margin: 0;
    font-family: var(--font-island-heading);
    font-size: 1.4rem;
    font-weight: 400;
  }
  .services__list p {
    max-width: 34rem;
    margin: 0;
    line-height: 1.8;
  }
  .services__list i {
    font-style: normal;
    transition: transform 0.25s;
  }
  .services__list a:hover i {
    transform: translateX(0.4rem);
  }
  .works {
    width: auto;
    max-width: none;
    padding-inline: max(4rem, calc((100% - 1180px) / 2));
    background: #26362a;
    color: #f5f3e9;
  }
  .works .section-heading > p {
    color: rgba(255, 255, 255, 0.55);
  }
  .works__grid,
  .journal__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
    margin-top: 4rem;
  }
  .works__grid a,
  .journal__grid a {
    color: inherit;
    text-decoration: none;
  }
  .works__grid figure,
  .journal__grid figure {
    aspect-ratio: 4/3;
    margin: 0 0 1.5rem;
    overflow: hidden;
  }
  .works__grid img,
  .journal__grid img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s;
  }
  .works__grid a:hover img,
  .journal__grid a:hover img {
    transform: scale(1.025);
  }
  .works__grid p,
  .journal__grid p {
    margin: 0 0 0.65rem;
    color: rgba(255, 255, 255, 0.55);
    font-size: 0.65rem;
    letter-spacing: 0.16em;
  }
  .works__grid h3,
  .journal__grid h3 {
    margin: 0;
    font-family: var(--font-island-heading);
    font-size: 1.3rem;
    font-weight: 400;
    line-height: 1.55;
  }
  .journal__intro {
    display: flex;
    justify-content: space-between;
    gap: 3rem;
    align-items: end;
    margin-top: 2rem;
  }
  .journal__intro > p {
    max-width: 35rem;
    margin: 0;
    line-height: 2;
  }
  .journal__intro .text-arrow {
    margin: 0;
    white-space: nowrap;
  }
  .journal__grid p {
    color: var(--island-muted);
  }
  .closing {
    position: relative;
    min-height: 680px;
    color: #fff;
  }
  .closing > img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .closing__copy {
    position: absolute;
    z-index: 1;
    left: max(4rem, calc((100% - 1180px) / 2));
    top: 50%;
    transform: translateY(-50%);
  }
  .closing__copy > p {
    font-size: 0.78rem;
    letter-spacing: 0.15em;
  }
  .closing h2 {
    margin: 1.5rem 0 2.5rem;
    font-family: var(--font-island-heading);
    font-size: clamp(2.2rem, 5vw, 4.5rem);
    font-weight: 400;
    line-height: 1.5;
    letter-spacing: 0.06em;
  }
  .closing a {
    display: inline-flex;
    justify-content: space-between;
    width: min(20rem, 80vw);
    padding: 1.2rem 1.4rem;
    border: 1px solid rgba(255, 255, 255, 0.7);
    color: #fff;
    text-decoration: none;
  }
  .reveal-section {
    opacity: 0;
    transform: translateY(24px);
    transition:
      opacity 0.7s,
      transform 0.7s;
  }
  .reveal-section[data-in-view="true"] {
    opacity: 1;
    transform: none;
  }
  @media (max-width: 850px) {
    .section-shell {
      width: calc(100% - 4rem);
    }
    .purpose {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
    .purpose__lead {
      margin-bottom: 3.5rem;
    }
    .about-block {
      grid-template-columns: 1fr;
    }
    .about-block figure {
      min-height: 65vw;
    }
    .about-block__copy {
      padding: 5rem 3rem;
    }
    .services__list a {
      grid-template-columns: 3rem 1fr 2rem;
      gap: 1rem;
    }
    .services__list p {
      grid-column: 2;
    }
    .services__list i {
      grid-column: 3;
      grid-row: 1 / 3;
    }
    .works {
      padding-inline: 2rem;
    }
    .works__grid,
    .journal__grid {
      gap: 1rem;
    }
  }
  @media (max-width: 600px) {
    .section-shell {
      width: calc(100% - 2rem);
    }
    .top-hero {
      min-height: 760px;
    }
    .top-hero__copy,
    .closing__copy {
      left: 1.25rem;
      right: 1.25rem;
    }
    .top-hero__copy h1 {
      font-size: clamp(2.8rem, 14vw, 4.2rem);
    }
    .purpose__themes {
      grid-template-columns: 1fr;
    }
    .purpose__themes article {
      padding: 1.5rem 0;
      border-bottom: 1px solid var(--island-border);
    }
    .purpose__themes h3 {
      margin: 0.8rem 0;
    }
    .about-block figure {
      min-height: 95vw;
    }
    .about-block__copy {
      padding: 4rem 1.25rem;
    }
    .about-block__copy strong br {
      display: none;
    }
    .services__list a {
      grid-template-columns: 2.2rem 1fr 1.5rem;
      padding: 2rem 0;
    }
    .services__list h3 {
      font-size: 1.2rem;
    }
    .works {
      padding-inline: 1rem;
    }
    .works__grid,
    .journal__grid {
      grid-template-columns: 1fr;
      gap: 3.5rem;
    }
    .journal__intro {
      display: block;
    }
    .journal__intro .text-arrow {
      margin-top: 2rem;
    }
    .closing {
      min-height: 620px;
    }
  }
  @media (prefers-reduced-motion: reduce) {
    :global(html) {
      scroll-behavior: auto;
    }
    .reveal-section {
      opacity: 1;
      transform: none;
      transition: none;
    }
  }

  /* Visual refinement: quieter type, denser photography, island-derived color. */
  .top-hero {
    min-height: min(800px, 100svh);
  }
  .top-hero__copy h1 {
    font-size: clamp(3.5rem, 6vw, 5.5rem);
    line-height: 1.22;
  }
  .section-heading h2,
  .about-block h2 {
    font-size: clamp(2.5rem, 3.5vw, 3rem);
    line-height: 1.45;
  }
  .purpose {
    grid-template-columns: minmax(14rem, 0.52fr) minmax(0, 1.48fr);
    gap: clamp(3rem, 7vw, 7rem);
    padding-block: clamp(5.5rem, 9vw, 8rem);
  }
  .purpose__lead {
    max-width: 38em;
    margin-bottom: 3.5rem;
    font-size: clamp(1.05rem, 1.5vw, 1.3rem);
    line-height: 1.95;
  }
  .purpose__themes {
    gap: clamp(0.8rem, 1.5vw, 1.5rem);
    border-top: 0;
  }
  .purpose__themes article {
    padding: 0;
  }
  .purpose__themes article:nth-child(2) {
    margin-top: 2.5rem;
  }
  .purpose__themes figure {
    position: relative;
    aspect-ratio: 3 / 4;
    margin: 0 0 1.5rem;
    overflow: hidden;
    border-radius: 0.75rem 0.75rem 2.5rem 0.75rem;
    background: #e5edcf;
  }
  .purpose__themes article:nth-child(2) figure {
    aspect-ratio: 4 / 5;
    border-radius: 2.5rem 0.75rem 0.75rem;
  }
  .purpose__themes article:nth-child(3) figure {
    aspect-ratio: 3 / 4;
    border-radius: 0.75rem 2.5rem 0.75rem 0.75rem;
  }
  .purpose__themes img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s ease;
  }
  .purpose__themes article:first-child img {
    object-position: 57% center;
  }
  .purpose__themes article:nth-child(3) img {
    object-position: 48% center;
  }
  .purpose__themes article:hover img {
    transform: scale(1.025);
  }
  .purpose__themes span {
    color: #687451;
    font-size: 0.62rem;
    letter-spacing: 0.14em;
  }
  .purpose__themes h3 {
    margin: 0.65rem 0 0.65rem;
    font-size: clamp(1.05rem, 1.5vw, 1.35rem);
    line-height: 1.6;
  }
  .about-block {
    min-height: 650px;
    background:
      linear-gradient(155deg, transparent 0 72%, rgba(207, 218, 178, 0.45) 72%),
      #eeecdf;
  }
  .about-block figure {
    min-height: 560px;
    margin: clamp(2rem, 4vw, 4rem) 0 clamp(2rem, 4vw, 4rem)
      clamp(2rem, 5vw, 5rem);
    overflow: hidden;
    border-radius: 0.75rem 4rem 0.75rem 0.75rem;
  }
  .about-block__copy {
    max-width: 38em;
    padding: clamp(3rem, 6vw, 6rem);
  }
  .about-block__copy > p:not(.section-label) {
    max-width: 36em;
    line-height: 1.95;
  }
  .services {
    width: auto;
    max-width: none;
    padding: clamp(5.5rem, 9vw, 8rem) max(4rem, calc((100% - 1180px) / 2));
    background:
      linear-gradient(175deg, transparent 0 80%, rgba(226, 218, 150, 0.28) 80%),
      #f7f5e9;
  }
  .services__list {
    margin-top: 3rem;
  }
  .services__list a {
    grid-template-columns:
      3rem 8.5rem minmax(12rem, 0.75fr) minmax(18rem, 1fr)
      2rem;
    gap: clamp(1rem, 2.5vw, 2.5rem);
    min-height: 9.5rem;
    padding: 1.25rem 0;
  }
  .services__list figure {
    aspect-ratio: 4 / 3;
    margin: 0;
    overflow: hidden;
    border-radius: 0.45rem 1.5rem 0.45rem 0.45rem;
  }
  .services__list a:nth-child(even) figure {
    border-radius: 1.5rem 0.45rem 0.45rem;
  }
  .services__list img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.45s ease;
  }
  .services__list a:hover img {
    transform: scale(1.035);
  }
  .services__list h3 {
    font-size: 1.25rem;
    line-height: 1.55;
  }
  .services__list p {
    max-width: 36em;
    line-height: 1.8;
  }
  .works,
  .journal {
    padding-block: clamp(5.5rem, 9vw, 8rem);
  }
  .works__grid,
  .journal__grid {
    gap: clamp(1rem, 2vw, 2rem);
    margin-top: 3rem;
  }
  .works__grid figure,
  .journal__grid figure {
    border-radius: 0.65rem 2.25rem 0.65rem 0.65rem;
  }
  .works__grid a:nth-child(2) {
    margin-top: 2.5rem;
  }
  .works__grid a:nth-child(2) figure,
  .journal__grid a:nth-child(2) figure {
    border-radius: 2.25rem 0.65rem 0.65rem;
  }
  .works__grid a:nth-child(3) figure {
    aspect-ratio: 3 / 4;
  }
  .journal {
    background: #f3f5e9;
  }
  .journal__intro > p {
    max-width: 36em;
    line-height: 1.9;
  }
  .journal__grid a:nth-child(2) {
    margin-top: 2.25rem;
  }
  .journal__grid figure {
    aspect-ratio: 16 / 11;
  }
  .closing {
    min-height: 600px;
  }
  .closing h2 {
    font-size: clamp(2.5rem, 4vw, 3.5rem);
    line-height: 1.45;
  }

  @media (max-width: 850px) {
    .purpose {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }
    .purpose .section-heading {
      display: grid;
      grid-template-columns: 1fr 1.5fr;
      gap: 2rem;
      align-items: start;
    }
    .purpose__themes article:nth-child(2) {
      margin-top: 1.5rem;
    }
    .about-block figure {
      margin: 2rem 2rem 0;
      min-height: 58vw;
    }
    .services {
      padding-inline: 2rem;
    }
    .services__list a {
      grid-template-columns: 2.5rem 7.5rem minmax(11rem, 0.8fr) 1fr 1.5rem;
      gap: 1rem;
    }
  }

  @media (max-width: 600px) {
    .top-hero {
      min-height: 660px;
    }
    .top-hero__copy h1 {
      font-size: clamp(2.5rem, 11vw, 2.875rem);
      line-height: 1.25;
    }
    .section-heading h2,
    .about-block h2 {
      font-size: clamp(1.75rem, 8vw, 2.125rem);
    }
    .purpose,
    .works,
    .journal {
      padding-block: 4.5rem;
    }
    .purpose .section-heading {
      display: block;
    }
    .purpose__lead {
      margin-bottom: 2.5rem;
      font-size: 1rem;
      line-height: 1.9;
    }
    .purpose__themes {
      gap: 2.5rem;
    }
    .purpose__themes article,
    .purpose__themes article:nth-child(2) {
      display: grid;
      grid-template-columns: minmax(0, 1.25fr) minmax(8rem, 0.75fr);
      gap: 1.25rem;
      align-items: center;
      margin: 0;
      padding: 0;
      border: 0;
    }
    .purpose__themes figure,
    .purpose__themes article:nth-child(2) figure,
    .purpose__themes article:nth-child(3) figure {
      aspect-ratio: 4 / 3;
      margin: 0;
      border-radius: 0.5rem 1.75rem 0.5rem 0.5rem;
    }
    .purpose__themes article:nth-child(even) figure {
      order: 2;
      border-radius: 1.75rem 0.5rem 0.5rem;
    }
    .purpose__themes h3 {
      font-size: 1.05rem;
      line-height: 1.55;
    }
    .purpose__themes article:first-child img {
      object-position: 54% center;
    }
    .purpose__themes article:nth-child(3) img {
      object-position: 45% center;
    }
    .about-block {
      min-height: 0;
    }
    .about-block figure {
      min-height: 85vw;
      margin: 1rem 1rem 0;
      border-radius: 0.5rem 2.5rem 0.5rem 0.5rem;
    }
    .about-block__copy {
      padding-block: 3.5rem;
    }
    .services {
      padding: 4.5rem 1rem;
    }
    .services__list {
      display: grid;
      gap: 1rem;
      border: 0;
    }
    .services__list a {
      display: grid;
      grid-template-columns: 2rem 1fr 1.5rem;
      gap: 0.7rem 0.75rem;
      min-height: 0;
      padding: 0 0 1.5rem;
      overflow: hidden;
      border: 0;
      border-radius: 0.65rem 1.75rem 0.65rem 0.65rem;
      background: rgba(255, 255, 255, 0.62);
    }
    .services__list figure {
      grid-column: 1 / -1;
      aspect-ratio: 16 / 9;
      border-radius: 0;
    }
    .services__list span {
      grid-column: 1;
      padding-left: 0.8rem;
    }
    .services__list h3 {
      grid-column: 2;
      font-size: 1.1rem;
    }
    .services__list p {
      grid-column: 2 / 4;
      padding-right: 1rem;
    }
    .services__list i {
      grid-column: 3;
      grid-row: 2;
    }
    .works__grid a:nth-child(2),
    .journal__grid a:nth-child(2) {
      margin-top: 0;
    }
    .works__grid a:nth-child(3) figure {
      aspect-ratio: 4 / 3;
    }
    .closing {
      min-height: 540px;
    }
    .closing h2 {
      font-size: clamp(1.9rem, 8.5vw, 2.35rem);
      line-height: 1.5;
    }
  }
</style>

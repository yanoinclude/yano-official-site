---
import BaseLayout from "@/layouts/BaseLayout.astro";
import YanoSpeechBubble from "@/components/animation/YanoSpeechBubble.astro";
import heroLemonGrove from "@/assets/images/hero-user-lemon-grove.jpg";
import lemonBasket from "@/assets/images/scenery/nakajima-lemon-basket.jpeg";
import weekendLemonJuice from "@/assets/images/product/weekend-lemon-juice.png";
import lemonTasting from "@/assets/images/lemon-farm/lemon-tasting.jpeg";
import youngLemon from "@/assets/images/lemon-farm/young-lemon.jpeg";
import harvestBoy from "@/assets/images/lemon-farm/harvest-boy.jpeg";
import lemonBlossom from "@/assets/images/lemon-farm/lemon-blossom.jpeg";
import harvestFriends from "@/assets/images/lemon-farm/harvest-friends.jpeg";
import orchardGroup from "@/assets/images/lemon-farm/orchard-group.jpeg";
import harvestCrates from "@/assets/images/lemon-farm/harvest-crates.jpeg";
import orchardRest from "@/assets/images/lemon-farm/orchard-rest.jpeg";

const seasons = [
  {
    label: "SPRING",
    title: "春",
    mark: "花",
    body: "花が咲き、新しい枝が伸びる季節。木の状態を確認しながら、枝や葉の混み具合を整えます。",
  },
  {
    label: "SUMMER",
    title: "夏",
    mark: "草",
    body: "草が大きく伸びる季節。草を刈り、木の足元を整え、レモンに光と風が届く環境をつくります。",
  },
  {
    label: "AUTUMN",
    title: "秋",
    mark: "実",
    body: "少しずつ実が大きくなる季節。色や形、枝の状態を見ながら、収穫までの変化を見守ります。",
  },
  {
    label: "WINTER",
    title: "冬",
    mark: "収",
    body: "黄色く色づいたレモンを、ひとつずつ手で収穫します。収穫した実は、生果や果汁など、次の楽しみへつなげます。",
  },
];

const freshLemon = {
  status: "preparing" as "preparing" | "available",
  url: "",
};

const farmScenes = [
  { image: harvestFriends, alt: "レモン畑で収穫かごを持つ仲間たち", caption: "仲間と迎えた、収穫の日。" },
  { image: lemonBlossom, alt: "葉の間で咲く白いレモンの花", caption: "春、白い花がひらく。" },
  { image: youngLemon, alt: "枝に実った小さな緑色のレモン", caption: "花のあとに生まれた、小さな実。" },
  { image: orchardGroup, alt: "レモン畑に集まった収穫メンバー", caption: "斜面の畑に、笑い声が増える。" },
  { image: harvestCrates, alt: "収穫したレモンを黄色いかごへ運ぶ様子", caption: "ひとつずつ、かごへ。" },
  { image: harvestBoy, alt: "収穫した大きなレモンを見せる少年", caption: "形も大きさも、それぞれ。" },
  { image: orchardRest, alt: "収穫したレモンを囲んで畑で休む二人", caption: "木陰で、ひと休み。" },
  { image: lemonTasting, alt: "畑で採れたレモンを味見する男性", caption: "採れたてを、その場で味わう。" },
];
---

<BaseLayout
  title="レモン農園｜矢野、週末移住中。"
  description="中島のレモン畑で続ける、手入れから収穫、商品になるまでの週末の営みを紹介します。"
>
  <article class="farm-page">
    <YanoSpeechBubble character="mowing" size="map" />
    <header class="farm-hero">
      <div class="farm-hero__copy">
        <p class="eyebrow">LEMON FARM</p>
        <h1>島の風と光で、<br /><span class="word-keep">レモンを育てる。</span></h1>
        <p class="farm-hero__lead">
          中島の斜面に広がるレモン畑。<br />週末ごとに木と向き合い、季節の変化を手で確かめています。
        </p>
        <a class="farm-hero__cta" href="#farm-intro-title">レモン畑をのぞく <span aria-hidden="true">→</span></a>
      </div>
      <div class="farm-stage" aria-label="レモンの光の中で畑を手入れする矢野">
        <svg class="farm-stage__lamp" viewBox="0 0 120 90" aria-hidden="true">
          <path class="farm-stage__leaf" d="M77 19c8-12 20-12 29-6-7 11-18 15-29 9Z" />
          <path class="farm-stage__lemon" d="M70 16c21 1 34 18 28 38S70 87 48 77 20 47 31 29c8-13 22-17 39-13Z" />
          <path class="farm-stage__shine" d="M47 31c-7 6-10 13-9 21" />
        </svg>
        <span class="farm-stage__beam" aria-hidden="true"></span>
        <span class="farm-stage__pool" aria-hidden="true"></span>
        <img class="farm-stage__shadow" src="/images/lemon-farm/yano-lemon-tree.png" alt="" aria-hidden="true" />
        <img class="farm-stage__art" src="/images/lemon-farm/yano-lemon-tree.png" alt="草刈機を持つ矢野と実をつけたレモンの木のイラスト" />
      </div>
    </header>

    <section class="farm-story" id="farm-intro-title" aria-labelledby="farm-story-title">
      <div class="farm-story__heading">
        <p class="eyebrow">STORY OF THE FARM</p>
        <h2 id="farm-story-title">切られるはずだった、<br />100本のレモンの木。</h2>
      </div>
      <figure class="farm-story__photo">
        <img src={heroLemonGrove.src} alt="中島の斜面に広がるレモン畑" loading="eager" />
      </figure>
      <div class="farm-story__body">
        <p>古民家の隣にあった、レモン畑。</p>
        <p>長く畑を守ってきた農家さんは、古民家再生を始めた頃から、活動を一番近くで見守ってくれた人でした。</p>
        <p>けれど、足を痛めてから、斜面での作業を続けることが難しくなっていきました。</p>
        <blockquote>「もう、レモンの木は全部切る」</blockquote>
        <p>そう聞いたとき、この風景を残したいと思いました。</p>
        <p>農業は初めて。それでも約100本の木を引き継ぎ、週末ごとに島へ通いながら、草を刈り、枝を見て、少しずつ手入れを続けています。</p>
      </div>
    </section>

    <section class="farm-facts" aria-labelledby="farm-facts-title">
      <header>
        <p class="eyebrow">ABOUT THE FARM</p>
        <h2 id="farm-facts-title">週末ごとに向き合う、<br />中島のレモン畑。</h2>
      </header>
      <dl>
        <div><dt>LOCATION</dt><dd>愛媛県松山市・中島</dd><span aria-hidden="true">船</span></div>
        <div><dt>LEMON TREES</dt><dd><b>約100本</b></dd><span aria-hidden="true">木</span></div>
        <div><dt>WEEKEND FARMING</dt><dd>週末ごとに手入れ</dd><span aria-hidden="true">刈</span></div>
        <div><dt>HARVEST</dt><dd>冬を中心に収穫</dd><span aria-hidden="true">実</span></div>
      </dl>
    </section>

    <section class="farm-seasons" aria-labelledby="farm-seasons-title">
      <header>
        <p class="eyebrow">SEASONS OF THE FARM</p>
        <h2 id="farm-seasons-title">週末を重ねて、<br />次の実りを待つ。</h2>
        <p>毎週できることは、決して多くありません。それでも畑へ通い、木の変化を確かめながら、その季節に必要な手入れを重ねています。</p>
      </header>
      <ol>
        {seasons.map((season) => (
          <li>
            <span class="farm-seasons__mark" aria-hidden="true">{season.mark}</span>
            <small>{season.label}</small>
            <h3>{season.title}</h3>
            <p>{season.body}</p>
          </li>
        ))}
      </ol>
    </section>

    <section class="farm-gallery" aria-labelledby="gallery-title">
      <div class="farm-gallery__heading">
        <p class="eyebrow">SCENES FROM THE FARM</p>
        <h2 id="gallery-title">畑の景色</h2>
      </div>
      <p class="gallery-note">潮風、斜面の日差し、木々の間を抜ける風。花が咲き、実が育ち、人が集まる。畑には、訪れるたびに違う景色があります。</p>
      <div class="farm-gallery__grid">
        {farmScenes.map((scene, index) => (
          <figure class:list={["gallery-scene", { "gallery-scene--wide": index === 0 }]}>
            <img src={scene.image.src} alt={scene.alt} loading="eager" decoding="async" />
            <figcaption>{scene.caption}</figcaption>
          </figure>
        ))}
      </div>
    </section>

    <section class="farm-to-bottle" aria-labelledby="farm-to-bottle-title">
      <header>
        <p class="eyebrow">FROM FARM TO BOTTLE</p>
        <h2 id="farm-to-bottle-title">畑の続きを、<br />一本に。</h2>
        <p>草を刈り、枝を整え、季節を越えて実ったレモン。収穫した実は、果汁や生果など、暮らしの中で島を楽しんでもらえる形へつなげています。</p>
        <p>商品をつくることも、この畑を次の季節へ残していくための手入れのひとつです。</p>
      </header>
      <ol aria-label="畑から商品になるまで">
        {["育てる", "収穫する", "搾る", "瓶に詰める", "届ける"].map((step, index) => (
          <li><span>{String(index + 1).padStart(2, "0")}</span><b>{step}</b></li>
        ))}
      </ol>
    </section>

    <section class="farm-product" aria-labelledby="product-title">
      <div class="farm-product__copy">
        <p class="eyebrow">WEEKEND LEMON</p>
        <h2 id="product-title">島との関係が始まる、<br />小さな一本。</h2>
        <p>愛媛・中島のレモンから生まれた、100％レモン果汁です。炭酸割り、料理、お酒、お風呂上がりの一杯に。日常の中で、島の果実を気軽に楽しんでいただけます。</p>
        <h3>週末移住レモン果汁</h3>
        <dl>
          <div><dt>内容量</dt><dd>150ml</dd></div>
          <div><dt>原材料名</dt><dd>レモン</dd></div>
          <div><dt>価格</dt><dd>800円</dd></div>
          <div><dt>保存方法</dt><dd>直射日光を避け、常温で保存</dd></div>
        </dl>
        <p class="farm-product__note">開栓後は10℃以下で保存し、早めに使用してください。商品の売上は、レモン畑の手入れや次の季節の活動につながります。</p>
        <a href="/product/">週末移住レモン果汁を見る <span aria-hidden="true">→</span></a>
      </div>
      <figure><img src={weekendLemonJuice.src} alt="週末移住レモンの商品" loading="eager" /></figure>
    </section>

    <section class="fresh-lemon" aria-labelledby="fresh-lemon-title">
      <figure><img src={lemonBasket.src} alt="中島の畑で収穫したレモン" loading="eager" /></figure>
      <div>
        <p class="eyebrow">FRESH LEMON</p>
        <h2 id="fresh-lemon-title">旬の中島レモンも、<br />季節に合わせて届けます。</h2>
        <p>中島の畑で収穫したレモンを、1kg単位で販売します。形や大きさはひとつずつ異なりますが、それも島の畑で育ったレモンの個性です。</p>
        <h3>中島レモン 1kg</h3>
        {freshLemon.status === "available" && freshLemon.url ? (
          <a href={freshLemon.url}>中島レモンを見る <span aria-hidden="true">→</span></a>
        ) : (
          <span class="fresh-lemon__status">販売準備中</span>
        )}
      </div>
    </section>

    <section class="join-farm" aria-labelledby="join-farm-title">
      <p class="eyebrow">JOIN THE FARM</p>
      <h2 id="join-farm-title">畑をのぞく。<br />収穫する。<br />一緒に手を動かす。</h2>
      <p>レモンの収穫や畑の見学など、季節と作業状況に合わせて、中島のレモンに触れる機会をつくっています。開催時期や内容は、Instagramでお知らせします。</p>
      <a href="https://www.instagram.com/kominka.lemon/" target="_blank" rel="noreferrer">Instagramで活動を見る <span aria-hidden="true">↗</span></a>
    </section>

    <section class="farm-final" aria-labelledby="farm-final-title">
      <p class="eyebrow">FROM NAKAJIMA TO YOUR TABLE</p>
      <h2 id="farm-final-title">この畑の続きを、<br />食卓へ。</h2>
      <p>週末ごとに手を入れながら育てた、中島のレモン。一本の果汁から、島との関係を始めてみませんか。</p>
      <div>
        <a href="/product/">週末移住レモン果汁を見る <span aria-hidden="true">→</span></a>
        <a href="https://www.instagram.com/kominka.lemon/" target="_blank" rel="noreferrer">Instagramで畑の様子を見る <span aria-hidden="true">↗</span></a>
      </div>
    </section>
  </article>
</BaseLayout>

<style>
  :global(body) { background: #36a9ee; }
  :global(body:has(.farm-page)) { --site-frame-space: clamp(8px, .7vw, 10px); }
  :global(body:has(.farm-page) .site-header) { display: none; }
  .farm-page { position: relative; overflow: clip; background: #36a9ee; color: #fff; }
  .word-keep { white-space: nowrap; }
  .eyebrow { margin: 0 0 1rem; color: #ffe000; font-size: .78rem; font-weight: 800; line-height: 1.4; letter-spacing: .16em; }
  .farm-hero { position: relative; display: grid; grid-template-columns: minmax(20rem, .82fr) minmax(0, 1.18fr); gap: clamp(2.5rem, 5vw, 4.5rem); align-items: center; min-height: 0; padding: clamp(20rem, 23vw, 22rem) max(2.5rem, calc((100vw - 1160px) / 2)) clamp(5rem, 6.5vw, 6rem); }
  .farm-hero h1, .farm-intro h2, .farm-gallery h2, .farm-product h2 { margin: 0; color: #fff; font-family: "Noto Serif JP", serif; font-weight: 500; letter-spacing: .01em; text-wrap: balance; }
  .farm-hero h1 { max-width: 11em; font-size: clamp(3rem, 3.6vw, 3.25rem); line-height: 1.4; }
  .farm-hero__lead { max-width: 34rem; margin: 1.5rem 0 0; font-size: 1rem; font-weight: 500; line-height: 1.9; }
  .farm-hero__image { position: relative; margin: 0; padding: .75rem; background: #fffaf0; box-shadow: .65rem .7rem 0 #ffe000; transform: rotate(1.2deg); }
  .farm-hero__image img { display: block; width: 100%; aspect-ratio: 4 / 3; object-fit: cover; }
  .farm-hero__image figcaption { padding: .75rem .25rem .15rem; color: #21466a; font-size: .68rem; font-weight: 800; letter-spacing: .18em; }
  .farm-intro { display: grid; grid-template-columns: 1.05fr .95fr; gap: clamp(3rem, 6vw, 6rem); width: min(calc(100% - 5rem), 1120px); margin: 0 auto; padding: clamp(5rem, 6.5vw, 6rem) 0; border-top: 1px solid rgb(255 255 255 / 48%); }
  .farm-intro h2, .farm-gallery h2, .farm-product h2 { max-width: 18em; font-size: clamp(2.125rem, 2.5vw, 2.25rem); line-height: 1.45; }
  .farm-intro > p { align-self: end; margin: 0; font-size: 1rem; font-weight: 500; line-height: 1.9; }
  .farm-process { display: grid; gap: clamp(2rem, 3vw, 2.5rem); width: min(calc(100% - 5rem), 1180px); margin: 0 auto; padding-bottom: clamp(5rem, 6.5vw, 6rem); }
  .process-card { display: grid; grid-template-columns: 45% 55%; min-height: 0; overflow: hidden; border: 2px solid #143d62; border-radius: 1.2rem; background: #fffaf0; color: #143d62; box-shadow: .5rem .55rem 0 #ffe000; }
  .process-card:nth-child(even) figure { order: 2; }
  .process-card figure { display: grid; place-items: center; margin: 0; overflow: hidden; background: #f4efe5; }
  .process-card figure img { display: block; width: 100%; height: 100%; aspect-ratio: 4 / 3; object-fit: cover; transition: transform .5s ease; }
  .process-card__copy { display: flex; flex-direction: column; justify-content: center; padding: clamp(2rem, 3.5vw, 3rem); }
  .process-card__copy > span { color: #e2b900; font-size: .82rem; font-weight: 900; letter-spacing: .18em; }
  .process-card h2 { margin: 1rem 0 1.25rem; font-family: "Noto Sans JP", sans-serif; font-size: clamp(1.375rem, 1.65vw, 1.5rem); font-weight: 500; line-height: 1.5; }
  .process-card p { max-width: 30rem; margin: 0; font-size: 1rem; font-weight: 500; line-height: 1.9; }
  .farm-gallery { width: min(calc(100% - 5rem), 1180px); margin: 0 auto; padding-bottom: clamp(5rem, 6.5vw, 6rem); }
  .farm-gallery__heading { margin-bottom: 1rem; }
  .gallery-note { max-width: 42rem; margin: 1.25rem 0 clamp(2.5rem, 4vw, 3rem); font-size: 1rem; line-height: 1.9; }
  .farm-gallery__grid { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: clamp(1.2rem, 2vw, 1.75rem); align-items: start; }
  .gallery-scene { margin: 0; padding: .55rem .55rem 1rem; background: #fffaf0; color: #143d62; box-shadow: .4rem .45rem 0 #ffe000; }
  .gallery-scene:nth-child(even) { transform: rotate(.55deg); }
  .gallery-scene:nth-child(odd) { transform: rotate(-.45deg); }
  .gallery-scene--wide { grid-column: span 2; grid-row: span 2; }
  .gallery-scene img { display: block; width: 100%; aspect-ratio: 4 / 3; object-fit: cover; }
  .gallery-scene figcaption { padding: .8rem .35rem 0; font-size: .8125rem; font-weight: 700; line-height: 1.65; }
  .farm-product { display: grid; grid-template-columns: 1fr .8fr; align-items: center; width: min(calc(100% - 5rem), 1120px); margin: 0 auto clamp(5rem, 6.5vw, 6rem); padding: clamp(3rem, 4vw, 4rem); border: 2px solid #143d62; border-radius: 1.2rem; background: #fffaf0; color: #143d62; box-shadow: .65rem .7rem 0 #ffe000; }
  .farm-product .eyebrow { color: #c7a300; }
  .farm-product h2 { color: #143d62; }
  .farm-product__copy > p:last-of-type { max-width: 32rem; margin: 1.5rem 0 2rem; font-size: .96rem; line-height: 2; }
  .farm-product a { box-sizing: border-box; display: inline-flex; gap: 2rem; align-items: center; min-height: 52px; padding: .65rem 1.2rem; border: 2px solid #143d62; border-radius: .55rem; background: #ffe000; color: #143d62; font-size: .9rem; font-weight: 800; text-decoration: none; }
  .farm-product figure { display: flex; justify-content: center; margin: 0; }
  .farm-product img { display: block; width: min(100%, 21rem); max-height: 30rem; object-fit: contain; }
  @media (hover: hover) { .process-card:hover figure img { transform: scale(1.025); } .farm-product a:hover { background: #143d62; color: #fff; } }
  @media (max-width: 767px) {
    :global(body:has(.farm-page)) { --site-frame-space: 5px; }
    .eyebrow { font-size: .72rem; }
    .farm-hero { display: flex; flex-direction: column; align-items: stretch; min-height: 0; padding: 14.5rem 20px 4rem; }
    .farm-hero h1 { max-width: 11em; font-size: 2.25rem; line-height: 1.45; }
    .farm-hero__lead br { display: none; }
    .farm-hero__lead { font-size: .95rem; }
    .farm-hero__image { margin-top: 1rem; padding: .45rem; box-shadow: .4rem .45rem 0 #ffe000; }
    .farm-intro { grid-template-columns: 1fr; gap: 2rem; width: calc(100% - 40px); padding: 4rem 0; }
    .farm-intro h2, .farm-gallery h2, .farm-product h2 { font-size: 1.75rem; line-height: 1.5; }
    .farm-intro > p { font-size: .95rem; }
    .farm-process { width: calc(100% - 40px); gap: 2rem; padding-bottom: 4rem; }
    .process-card { grid-template-columns: 1fr; min-height: 0; box-shadow: .35rem .4rem 0 #ffe000; }
    .process-card:nth-child(even) figure { order: 0; }
    .process-card figure { aspect-ratio: 4 / 3; }
    .process-card__copy { padding: 1.75rem 1.4rem 2rem; }
    .process-card h2 { font-size: 1.3125rem; }
    .process-card p { font-size: .95rem; }
    .farm-gallery { width: calc(100% - 40px); padding-bottom: 4rem; }
    .farm-gallery__grid { grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 1.5rem 1rem; }
    .gallery-scene, .gallery-scene--wide { grid-column: span 1; grid-row: auto; }
    .gallery-scene--wide, .gallery-scene:last-child { grid-column: 1 / -1; }
    .gallery-note { margin: 1rem 0 2.5rem; font-size: .95rem; }
    .farm-product { grid-template-columns: 1fr; width: calc(100% - 40px); margin-bottom: 4rem; padding: 2rem 1.4rem; box-shadow: .4rem .45rem 0 #ffe000; }
    .farm-product figure { margin-top: 2rem; }
    .farm-product a { justify-content: space-between; width: 100%; min-height: 52px; }
  }
  @media (prefers-reduced-motion: reduce) { .process-card figure img { transition: none; } }

  /* Lemon spotlight hero. */
  .farm-page {
    border-top: 4px solid #ffe000;
  }

  .farm-hero {
    box-sizing: border-box;
    grid-template-columns: minmax(22rem, 0.75fr) minmax(36rem, 1.25fr);
    min-height: calc(100svh - 4px);
    padding: clamp(9.5rem, 13vh, 11rem) clamp(3rem, 7vw, 7rem) clamp(4rem, 7vh, 6rem);
    gap: clamp(2rem, 4vw, 5rem);
    overflow: hidden;
  }

  .farm-hero__copy {
    position: relative;
    z-index: 8;
    align-self: center;
  }

  .farm-hero h1 {
    max-width: 11em;
    font-size: clamp(3rem, 4vw, 3.6rem);
    line-height: 1.5;
    letter-spacing: 0.045em;
  }

  .farm-hero__lead {
    margin-top: 2rem;
  }

  .farm-hero__cta {
    position: relative;
    display: inline-flex;
    align-items: center;
    gap: 1.4rem;
    min-height: 48px;
    margin-top: 2.4rem;
    color: #fff;
    font-size: 0.96rem;
    font-weight: 800;
    text-decoration: none;
  }

  .farm-hero__cta::after {
    position: absolute;
    right: 0;
    bottom: 0.2rem;
    left: 0;
    height: 3px;
    background: #ffe000;
    content: "";
    transition: right 300ms ease;
  }

  .farm-hero__cta span {
    transition: transform 300ms ease;
  }

  .farm-stage {
    position: relative;
    align-self: stretch;
    min-height: min(67vw, 47rem);
    isolation: isolate;
  }

  .farm-stage__lamp {
    position: absolute;
    z-index: 6;
    top: 1%;
    right: 0;
    width: clamp(4.5rem, 7vw, 6.5rem);
    overflow: visible;
    filter: drop-shadow(0 4px 0 rgb(20 61 98 / 22%));
    transform: translateY(-16px) rotate(-4deg);
    opacity: 0;
    animation: lamp-arrive 500ms 180ms ease-out forwards;
    transform-origin: 50% 50%;
  }

  .farm-stage__lemon {
    fill: #ffe000;
    stroke: #143d62;
    stroke-linejoin: round;
    stroke-width: 5;
  }

  .farm-stage__leaf {
    fill: #5e8b45;
    stroke: #143d62;
    stroke-linejoin: round;
    stroke-width: 4;
  }

  .farm-stage__shine {
    fill: none;
    stroke: #fff7ad;
    stroke-linecap: round;
    stroke-width: 5;
  }

  .farm-stage__beam {
    position: absolute;
    z-index: 1;
    top: 8%;
    right: 2%;
    width: 86%;
    height: 77%;
    background:
      linear-gradient(150deg, rgb(255 246 161 / 3%) 3%, rgb(255 240 113 / 23%) 67%, rgb(255 235 76 / 14%));
    clip-path: polygon(93% 0, 100% 2%, 87% 100%, 2% 100%);
    filter: blur(1.5px);
    transform: scaleX(0.08);
    transform-origin: 96% 0;
    animation: beam-open 760ms 470ms cubic-bezier(.2, .7, .2, 1) forwards;
  }

  .farm-stage__pool {
    position: absolute;
    z-index: 2;
    right: 1%;
    bottom: 4%;
    width: 96%;
    height: 23%;
    border-radius: 50%;
    background: rgb(255 237 120 / 58%);
    filter: blur(1px);
    opacity: 0;
    animation: pool-arrive 500ms 850ms ease-out forwards;
  }

  .farm-stage__art,
  .farm-stage__shadow {
    position: absolute;
    right: -1%;
    bottom: 5%;
    width: min(100%, 56rem);
    height: 81%;
    object-fit: contain;
    object-position: right bottom;
  }

  .farm-stage__art {
    z-index: 5;
    opacity: 0;
    transform: translateY(10px);
    animation: art-arrive 560ms 760ms ease-out forwards;
  }

  .farm-stage__shadow {
    z-index: 3;
    filter: brightness(0) saturate(100%) invert(19%) sepia(25%) saturate(1692%) hue-rotate(171deg) brightness(83%) blur(1.5px);
    opacity: 0;
    transform: translate(-22%, 25%) scaleY(0.25) skewX(34deg);
    transform-origin: 55% 96%;
    animation: shadow-grow 620ms 980ms ease-out forwards;
  }

  .farm-hero:has(.farm-hero__cta:hover) .farm-stage__beam,
  .farm-hero:has(.farm-hero__cta:focus-visible) .farm-stage__beam {
    filter: brightness(1.12) blur(1px);
  }

  .farm-hero:has(.farm-hero__cta:hover) .farm-stage__lamp,
  .farm-hero:has(.farm-hero__cta:focus-visible) .farm-stage__lamp {
    transform: rotate(2deg);
  }

  .farm-hero__cta:hover span,
  .farm-hero__cta:focus-visible span {
    transform: translateX(6px);
  }

  .farm-hero__cta:hover::after,
  .farm-hero__cta:focus-visible::after {
    right: -0.75rem;
  }

  :global(.farm-page > .yano-brand) {
    top: 2rem;
    left: clamp(3rem, 6vw, 6rem);
  }

  :global(.farm-page > .yano-brand .yano-brand__bubble) {
    font-size: clamp(1.05rem, 1.5vw, 1.35rem);
  }

  :global(.farm-page > .yano-brand .yano-brand__character) {
    display: none;
  }

  @keyframes lamp-arrive {
    to { opacity: 1; transform: translateY(0) rotate(-2deg); }
  }

  @keyframes beam-open {
    to { transform: scaleX(1); }
  }

  @keyframes pool-arrive {
    to { opacity: 1; }
  }

  @keyframes art-arrive {
    to { opacity: 1; transform: translateY(0); }
  }

  @keyframes shadow-grow {
    to { opacity: 0.28; transform: translate(-22%, 25%) scaleY(0.25) skewX(34deg); }
  }

  @media (max-width: 900px) {
    .farm-hero {
      grid-template-columns: minmax(18rem, 0.78fr) minmax(27rem, 1.22fr);
      padding-inline: 2.5rem;
    }
  }

  @media (max-width: 767px) {
    .farm-page {
      border-top-width: 3px;
    }

    .farm-hero {
      display: flex;
      min-height: 820px;
      padding: 8.75rem 1.25rem 3rem;
    }

    .farm-hero h1 {
      font-size: clamp(2rem, 9.3vw, 2.35rem);
      line-height: 1.5;
    }

    .farm-hero__copy {
      position: static;
    }

    .farm-hero__lead {
      margin-top: 1.25rem;
      font-size: 0.95rem;
    }

    .farm-hero__lead br {
      display: none;
    }

    .farm-stage {
      width: 100%;
      min-height: 25rem;
      margin-top: 0.75rem;
    }

    .farm-stage__lamp {
      top: 0;
      right: 1%;
      width: 4.35rem;
    }

    .farm-stage__beam {
      top: 8%;
      right: -2%;
      width: 98%;
      height: 78%;
      clip-path: polygon(91% 0, 100% 2%, 97% 100%, 0 100%);
    }

    .farm-stage__pool {
      right: -6%;
      bottom: 3%;
      width: 108%;
      height: 20%;
    }

    .farm-stage__art,
    .farm-stage__shadow {
      right: -9%;
      bottom: 4%;
      width: 112%;
      height: 78%;
    }

    .farm-stage__shadow {
      transform: translate(-10%, 18%) scaleY(0.2) skewX(26deg);
    }

    .farm-hero__cta {
      position: absolute;
      z-index: 10;
      bottom: 1.25rem;
      left: 1.25rem;
      margin: 0;
    }

    :global(.farm-page > .yano-brand) {
      top: 1rem;
      left: 1rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .farm-stage__lamp,
    .farm-stage__beam,
    .farm-stage__pool,
    .farm-stage__art,
    .farm-stage__shadow {
      animation: none;
    }

    .farm-stage__lamp { opacity: 1; transform: none; }
    .farm-stage__beam { transform: none; }
    .farm-stage__pool { opacity: 1; }
    .farm-stage__art { opacity: 1; transform: none; }
    .farm-stage__shadow { opacity: 0.28; }
  }

  /* The farm story continues below the unchanged spotlight hero. */
  .farm-story,
  .farm-facts,
  .farm-seasons,
  .farm-to-bottle,
  .fresh-lemon,
  .join-farm,
  .farm-final {
    box-sizing: border-box;
  }

  .farm-story h2,
  .farm-facts h2,
  .farm-seasons h2,
  .farm-to-bottle h2,
  .fresh-lemon h2,
  .join-farm h2,
  .farm-final h2 {
    margin: 0;
    font-family: "Noto Serif JP", serif;
    font-size: clamp(2.125rem, 2.8vw, 2.5rem);
    font-weight: 500;
    line-height: 1.45;
    letter-spacing: 0.025em;
    text-wrap: pretty;
  }

  .farm-story {
    position: relative;
    display: grid;
    grid-template-columns: minmax(0, 0.9fr) minmax(24rem, 1.1fr);
    grid-template-areas: "heading photo" "body photo";
    align-items: center;
    gap: 2rem clamp(3rem, 6vw, 6rem);
    width: 100%;
    padding: clamp(6.5rem, 8vw, 8rem) max(2.5rem, calc((100vw - 1180px) / 2));
    background: #fffdf5;
    color: #143d62;
  }

  .farm-story::before {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: clamp(2rem, 3vw, 2.75rem);
    background: url("/images/decor/kominka-wave-divider.svg") repeat-x center / auto 100%;
    content: "";
  }

  .farm-story__heading { grid-area: heading; align-self: end; }
  .farm-story__photo { grid-area: photo; margin: 0; }
  .farm-story__body { grid-area: body; align-self: start; max-width: 38rem; }

  .farm-story__photo img {
    width: 100%;
    aspect-ratio: 4 / 5;
    object-fit: cover;
    object-position: 58% center;
    border: 2px solid #143d62;
    box-shadow: 0.6rem 0.65rem 0 #ffe000;
  }

  .farm-story__body p {
    margin: 0;
    font-size: 1rem;
    line-height: 1.9;
  }

  .farm-story__body p + p {
    margin-top: 1.1rem;
  }

  .farm-story__body blockquote {
    margin: 1.5rem 0;
    padding-left: 1.2rem;
    border-left: 5px solid #ffe000;
    font-family: "Noto Serif JP", serif;
    font-size: 1.35rem;
    font-weight: 500;
    line-height: 1.7;
  }

  .farm-facts {
    display: grid;
    grid-template-columns: minmax(18rem, 0.7fr) minmax(0, 1.3fr);
    gap: clamp(3rem, 6vw, 6rem);
    width: min(calc(100% - 5rem), 1180px);
    margin: 0 auto;
    padding: clamp(5rem, 7vw, 6.5rem) 0;
  }

  .farm-facts dl {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
    margin: 0;
  }

  .farm-facts dl > div {
    position: relative;
    min-height: 9rem;
    padding: 1.35rem;
    overflow: hidden;
    border: 2px solid #143d62;
    background: #fffdf5;
    color: #143d62;
    box-shadow: 0.35rem 0.4rem 0 #ffe000;
  }

  .farm-facts dt {
    color: #947b00;
    font-size: 0.72rem;
    font-weight: 900;
    letter-spacing: 0.13em;
  }

  .farm-facts dd {
    max-width: 12rem;
    margin: 1rem 0 0;
    font-size: 1.05rem;
    font-weight: 700;
    line-height: 1.55;
  }

  .farm-facts dd b {
    font-size: 1.55rem;
  }

  .farm-facts dl span {
    position: absolute;
    right: 1rem;
    bottom: 0.8rem;
    display: grid;
    place-items: center;
    width: 2.8rem;
    aspect-ratio: 1;
    border: 2px solid #143d62;
    border-radius: 50%;
    background: #ffe000;
    font-size: 0.75rem;
    font-weight: 900;
  }

  .farm-seasons {
    padding: clamp(5rem, 7vw, 6.5rem) max(2.5rem, calc((100vw - 1180px) / 2));
    background: #fffdf5;
    color: #143d62;
  }

  .farm-seasons header {
    display: grid;
    grid-template-columns: 0.8fr 1.2fr;
    gap: 1rem clamp(3rem, 6vw, 6rem);
    margin-bottom: 3rem;
  }

  .farm-seasons header .eyebrow,
  .farm-seasons header h2 {
    grid-column: 1;
  }

  .farm-seasons header > p:last-child {
    grid-column: 2;
    grid-row: 1 / span 2;
    align-self: end;
    max-width: 42rem;
    margin: 0;
    font-size: 1rem;
    line-height: 1.9;
  }

  .farm-seasons ol {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 1rem;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .farm-seasons li {
    position: relative;
    padding: 1.6rem 1.4rem;
    border-top: 3px solid #143d62;
    background: #fff;
  }

  .farm-seasons__mark {
    display: grid;
    place-items: center;
    width: 3rem;
    aspect-ratio: 1;
    margin-bottom: 1.5rem;
    border: 2px solid #143d62;
    border-radius: 52% 48% 55% 45%;
    background: #ffe000;
    font-weight: 900;
  }

  .farm-seasons small {
    color: #947b00;
    font-size: 0.7rem;
    font-weight: 900;
    letter-spacing: 0.13em;
  }

  .farm-seasons h3 {
    margin: 0.45rem 0 0.8rem;
    font-family: "Noto Serif JP", serif;
    font-size: 1.45rem;
    font-weight: 500;
  }

  .farm-seasons li p {
    margin: 0;
    font-size: 0.95rem;
    line-height: 1.85;
  }

  .farm-to-bottle {
    padding: clamp(5rem, 7vw, 6.5rem) max(2.5rem, calc((100vw - 1180px) / 2));
    background: #ffe66a;
    color: #143d62;
  }

  .farm-to-bottle header {
    display: grid;
    grid-template-columns: 0.8fr 1.2fr;
    gap: 0.75rem clamp(3rem, 6vw, 6rem);
  }

  .farm-to-bottle header .eyebrow,
  .farm-to-bottle header h2 {
    grid-column: 1;
  }

  .farm-to-bottle header > p {
    grid-column: 2;
    margin: 0;
    font-size: 1rem;
    line-height: 1.9;
  }

  .farm-to-bottle header > p:first-of-type {
    grid-row: 1 / span 2;
    align-self: end;
  }

  .farm-to-bottle header > p:last-of-type {
    margin-top: 0.75rem;
  }

  .farm-to-bottle .eyebrow {
    color: #143d62;
  }

  .farm-to-bottle ol {
    display: grid;
    grid-template-columns: repeat(5, minmax(0, 1fr));
    gap: 2.3rem;
    margin: 3.5rem 0 0;
    padding: 0;
    list-style: none;
  }

  .farm-to-bottle li {
    position: relative;
    display: grid;
    place-items: center;
    min-height: 7.5rem;
    border: 2px solid #143d62;
    border-radius: 50%;
    background: #fffdf5;
    text-align: center;
  }

  .farm-to-bottle li:not(:last-child)::after {
    position: absolute;
    left: calc(100% + 0.55rem);
    content: "→";
    font-size: 1.5rem;
  }

  .farm-to-bottle li span {
    font-size: 0.65rem;
    font-weight: 900;
    letter-spacing: 0.12em;
  }

  .farm-to-bottle li b {
    font-size: 0.95rem;
  }

  .farm-product {
    grid-template-columns: 1.08fr 0.92fr;
    width: min(calc(100% - 5rem), 1120px);
    margin: clamp(5rem, 7vw, 6.5rem) auto;
    padding: clamp(2.5rem, 4vw, 4rem);
  }

  .farm-product__copy > p:nth-of-type(2) {
    max-width: 36rem;
    margin: 1.5rem 0 0;
    font-size: 1rem;
    line-height: 1.9;
  }

  .farm-product h3 {
    margin: 2rem 0 1rem;
    font-family: "Noto Serif JP", serif;
    font-size: 1.45rem;
    font-weight: 600;
  }

  .farm-product dl {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0;
    margin: 0 0 1.5rem;
    border-top: 1px solid rgb(20 61 98 / 28%);
  }

  .farm-product dl div {
    display: grid;
    grid-template-columns: 5rem 1fr;
    gap: 0.75rem;
    padding: 0.65rem 0;
    border-bottom: 1px solid rgb(20 61 98 / 28%);
  }

  .farm-product dt,
  .farm-product dd {
    margin: 0;
    font-size: 0.82rem;
    line-height: 1.7;
  }

  .farm-product dt {
    font-weight: 800;
  }

  .farm-product__note {
    margin: 0 0 1.5rem;
    font-size: 0.8rem;
    line-height: 1.75;
  }

  .fresh-lemon {
    display: grid;
    grid-template-columns: minmax(18rem, 0.7fr) minmax(0, 1.3fr);
    align-items: center;
    gap: clamp(3rem, 6vw, 6rem);
    padding: clamp(5rem, 7vw, 6.5rem) max(2.5rem, calc((100vw - 1120px) / 2));
    background: #fffdf5;
    color: #143d62;
  }

  .fresh-lemon figure {
    margin: 0;
  }

  .fresh-lemon figure img {
    width: 100%;
    aspect-ratio: 4 / 3;
    object-fit: cover;
    border: 2px solid #143d62;
    box-shadow: 0.45rem 0.5rem 0 #ffe000;
  }

  .fresh-lemon > div > p:not(.eyebrow) {
    max-width: 38rem;
    margin: 1.5rem 0;
    font-size: 1rem;
    line-height: 1.9;
  }

  .fresh-lemon h3 {
    margin: 0 0 1rem;
    font-size: 1.2rem;
  }

  .fresh-lemon__status {
    display: inline-flex;
    min-height: 48px;
    align-items: center;
    padding: 0.6rem 1rem;
    border: 2px solid #143d62;
    background: #eee9dc;
    font-weight: 800;
  }

  .join-farm {
    width: min(calc(100% - 5rem), 1120px);
    margin: 0 auto;
    padding: clamp(5rem, 7vw, 6.5rem) 0;
  }

  .join-farm h2 {
    max-width: 16em;
  }

  .join-farm > p:not(.eyebrow) {
    max-width: 45rem;
    margin: 1.5rem 0 2rem;
    font-size: 1rem;
    line-height: 1.9;
  }

  .join-farm > a {
    display: inline-flex;
    gap: 1.5rem;
    align-items: center;
    min-height: 50px;
    padding: 0.65rem 1.1rem;
    border: 2px solid #fff;
    background: #fffdf5;
    color: #143d62;
    font-weight: 800;
    text-decoration: none;
  }

  .farm-final {
    padding: clamp(5rem, 7vw, 6.5rem) max(2.5rem, calc((100vw - 1120px) / 2));
    background: #143d62;
    color: #fff;
  }

  .farm-final > p:not(.eyebrow) {
    max-width: 42rem;
    margin: 1.5rem 0 2rem;
    font-size: 1rem;
    line-height: 1.9;
  }

  .farm-final div {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .farm-final a {
    display: inline-flex;
    gap: 1.5rem;
    align-items: center;
    min-height: 52px;
    padding: 0.65rem 1.1rem;
    border: 2px solid #fff;
    color: #fff;
    font-weight: 800;
    text-decoration: none;
  }

  .farm-final a:first-child {
    border-color: #ffe000;
    background: #ffe000;
    color: #143d62;
  }

  @media (max-width: 767px) {
    .farm-story {
      grid-template-columns: 1fr;
      grid-template-areas: "heading" "photo" "body";
      gap: 2rem;
      padding: 5.5rem 1.25rem 4.5rem;
    }

    .farm-story__photo img {
      aspect-ratio: 4 / 3;
    }

    .farm-story__body blockquote {
      font-size: 1.2rem;
    }

    .farm-story h2,
    .farm-facts h2,
    .farm-seasons h2,
    .farm-to-bottle h2,
    .fresh-lemon h2,
    .join-farm h2,
    .farm-final h2 {
      font-size: 1.75rem;
    }

    .farm-facts {
      grid-template-columns: 1fr;
      gap: 2.5rem;
      width: calc(100% - 40px);
      padding: 4rem 0;
    }

    .farm-facts dl {
      gap: 0.8rem;
    }

    .farm-facts dl > div {
      min-height: 8.5rem;
      padding: 1.1rem;
    }

    .farm-facts dd {
      font-size: 0.95rem;
    }

    .farm-seasons,
    .farm-to-bottle,
    .fresh-lemon,
    .farm-final {
      padding: 4rem 1.25rem;
    }

    .farm-seasons header,
    .farm-to-bottle header {
      display: block;
    }

    .farm-seasons header > p:last-child,
    .farm-to-bottle header > p {
      margin-top: 1.5rem;
    }

    .farm-seasons ol {
      grid-template-columns: 1fr;
      gap: 0;
      margin-top: 2.5rem;
      border-left: 3px solid #ffe000;
    }

    .farm-seasons li {
      display: grid;
      grid-template-columns: 3rem 1fr;
      column-gap: 1rem;
      padding: 1.25rem 0 1.5rem 1.25rem;
      border-top: 0;
      border-bottom: 1px solid rgb(20 61 98 / 18%);
    }

    .farm-seasons__mark {
      grid-row: 1 / span 3;
      margin: 0;
    }

    .farm-seasons li small,
    .farm-seasons li h3,
    .farm-seasons li p {
      grid-column: 2;
    }

    .farm-to-bottle ol {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 1rem;
      margin-top: 2.5rem;
    }

    .farm-to-bottle li {
      min-height: 7rem;
    }

    .farm-to-bottle li:not(:last-child)::after {
      display: none;
    }

    .farm-product {
      grid-template-columns: 1fr;
      width: calc(100% - 40px);
      margin: 4rem auto;
      padding: 2rem 1.25rem;
    }

    .farm-product dl {
      grid-template-columns: 1fr;
    }

    .fresh-lemon {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .join-farm {
      width: calc(100% - 40px);
      padding: 4rem 0;
    }

    .join-farm > a,
    .farm-final a {
      box-sizing: border-box;
      justify-content: space-between;
      width: 100%;
    }

    .farm-final div {
      display: grid;
    }
  }
</style>

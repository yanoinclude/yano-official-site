<a
  class="event-badge"
  href="/events/nakajima-bbq-2026/"
  aria-label="8月8日 中島BBQイベントの特設ページを見る"
>
  <span class="event-badge__date">8/8 SAT</span>
  <strong>BBQ開催！</strong>
</a>

<style>
  .event-badge {
    position: absolute;
    z-index: 14;
    top: 12%;
    right: 9%;
    display: grid;
    grid-template-columns: 1fr;
    align-items: center;
    min-width: clamp(12.5rem, 17vw, 16.5rem);
    padding: 0.8rem 1rem 0.75rem;
    border: 3px solid #203b60;
    border-radius: 0.55rem;
    background: #ffe500;
    color: #203b60;
    text-decoration: none;
    box-shadow: 0.35rem 0.4rem 0 rgb(32 59 96 / 22%);
    transform: rotate(2deg);
    transition:
      transform 180ms ease,
      box-shadow 180ms ease;
  }

  .event-badge__date {
    grid-column: 1 / -1;
    margin-bottom: 0.18rem;
    font-family: var(--font-island-accent);
    font-size: 0.7rem;
    font-weight: 500;
    letter-spacing: 0.15em;
  }

  .event-badge strong {
    font-family: var(--font-island-heading);
    font-size: clamp(1.15rem, 1.65vw, 1.55rem);
    line-height: 1.2;
    white-space: nowrap;
  }

  .event-badge:hover,
  .event-badge:focus-visible {
    box-shadow: 0.18rem 0.22rem 0 rgb(32 59 96 / 22%);
    transform: translateY(3px) rotate(0deg);
  }

  @media (max-width: 767px) {
    .event-badge {
      top: 10%;
      right: 3%;
      left: auto;
      min-width: 8.8rem;
      padding: 0.55rem 0.65rem 0.5rem;
      border-width: 2px;
      box-shadow: 0.2rem 0.24rem 0 rgb(32 59 96 / 22%);
      transform: rotate(1deg);
    }

    .event-badge__date {
      font-size: 0.56rem;
    }

    .event-badge strong {
      font-size: 0.92rem;
    }

    .event-badge:hover,
    .event-badge:focus-visible {
      transform: translateY(2px) rotate(0deg);
    }
  }
</style>

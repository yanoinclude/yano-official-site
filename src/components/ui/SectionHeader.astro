---
type Props = {
  eyebrow?: string;
  heading: string;
  body?: string;
};

const { eyebrow, heading, body } = Astro.props;
---

<header class="section-header">
  {eyebrow && <p class="eyebrow">{eyebrow}</p>}
  <h2>{heading}</h2>
  {body && <p>{body}</p>}
</header>
